# PostgreSQL connection string (from Render PostgreSQL dashboard)
# Format: postgresql://user:password@hostname:5432/database
DATABASE_URL="postgresql://user:password@localhost:5432/if0_40532276_atelier"

# JWT Secret for authentication tokens (use a strong random string)
JWT_SECRET="your-secure-jwt-secret-change-me"

# Server port (Render will set this automatically)
PORT=5001

# Node environment
NODE_ENV=production

# CORS origin (update to your deployed frontend URL)
CORS_ORIGIN="https://your-frontend-on-render.onrender.com"
